using UnityEngine;
using System.Collections;

[System.Serializable]
public class CameraViewInfo
{
	/*
	private CameraController.CameraViewType type;
	private Vector3 cameraPosition;
	private Quaternion cameraRotation;
	private Vector3 orientationPos;
	private Quaternion orientationRot;
	private float camDistance;
	
	public CameraViewInfo (CameraController.CameraViewType type, Transform camReference) 
		: this (type, camReference.position, camReference.rotation) {}

	public CameraViewInfo (CameraController.CameraViewType type, Transform camReference, Transform orientationReference)
		: this (type, camReference.position, camReference.rotation, orientationReference.position, orientationReference.rotation) {}

	public CameraViewInfo (CameraController.CameraViewType type, Vector3 cameraPosition, Quaternion cameraRotation)
	{
		this.type = type;
		this.cameraPosition = cameraPosition;
		this.cameraRotation = cameraRotation;
	}

	public CameraViewInfo (CameraController.CameraViewType type, Vector3 cameraPosition, Quaternion cameraRotation, 
		Vector3 orientationPos, Quaternion orientationRot)
	{
		this.type = type;
		this.cameraPosition = cameraPosition;
		this.cameraRotation = cameraRotation;
		this.orientationPos = orientationPos;
		this.orientationRot = orientationRot;
	}

	public CameraViewInfo (CameraController.CameraViewType type, Vector3 cameraPosition, Quaternion cameraRotation, 
		Vector3 orientationPos, Quaternion orientationRot, float camDistance)
	{
		this.type = type;
		this.cameraPosition = cameraPosition;
		this.cameraRotation = cameraRotation;
		this.orientationPos = orientationPos;
		this.orientationRot = orientationRot;
		this.camDistance = camDistance;
	}

	public CameraController.CameraViewType Type
	{
		get{return type;}
	}

	public Vector3 CameraPosition
	{
		get{return cameraPosition;}
	}

	public Quaternion CameraRotation
	{
		get{return cameraRotation;}
	}

	public Vector3 OrientationPos
	{
		get{return orientationPos;}
	}

	public Quaternion OrientationRot
	{
		get{return orientationRot;}
	}

	public float CamDistance
	{
		get{return camDistance;}
	}
	*/
}
